import React from "react";
import styled from "styled-components";
import { useNavigate } from "react-router-dom";

const Login = (props) => {

    const navigate = useNavigate();

    return (
        <div>


            로그인 페이지
            {

                /*로그인 페이지
                로그인 여부 체크 -> 퀴즈 메인페이지로 보내기
                로그인 후 퀴즈 메인페이지로 보내기


                이메일 인풋
                패스워드 인풋

                비밀번호 찾기
                회원가입 버튼
                소셜 로그인 버튼(아이콘)
                */
            }
        </div>
    )
}

export default Login;
