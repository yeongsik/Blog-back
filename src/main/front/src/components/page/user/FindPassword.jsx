import React from "react";
import styled from "styled-components";

const FindPassword = (props) => {
    return (
        <div>
            {/* 비밀번호 찾기 페이지
                이메일 입력 -> 이메일로 임시 비밀번호 전송 -> 링크 클릭하면 새로운 비밀번호 입력하는 창 노출
            */}
        </div>
    );
}

export default FindPassword;
